# Server Port (Render / hosting services automatically set PORT)
PORT=4000

# Client origin allowed for CORS (e.g. https://your-app-name.vercel.app or comma-separated)
CLIENT_ORIGIN=https://echo-game.vercel.app,http://localhost:3000
