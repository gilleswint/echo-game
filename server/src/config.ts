export const PORT = process.env.PORT || 4000;
export const CLIENT_ORIGIN = process.env.CLIENT_ORIGIN || '*';
export const TURN_TIME_SECONDS = 30; // Seconds per clue turn
export const DISCUSSION_TIME_SECONDS = 60; // Discussion timer
export const VOTING_TIME_SECONDS = 45; // Voting timer
export const ROLE_REVEAL_TIME_SECONDS = 15; // Auto progress if idle
export const REVEAL_TIME_SECONDS = 15; // View votes before gameover
export const GUESS_TIME_SECONDS = 30; // Imposter guess timer
